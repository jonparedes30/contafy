# Render Blueprint para CONTAFY
services:
  - type: web
    name: contafy
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health/
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DJANGO_SETTINGS_MODULE
        value: core.settings
      - key: DATABASE_URL
        fromDatabase:
          name: contafy-db
          property: connectionString
      - key: ALLOWED_HOSTS
        value: ".onrender.com,localhost,127.0.0.1"
      - key: RENDER
        value: "true"
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_EMAIL
        value: admin@contafy.com
      - key: ADMIN_PASSWORD
        sync: false

databases:
  - name: contafy-db
    databaseName: contafy
    user: contafy
    plan: starter
